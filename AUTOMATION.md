# 创作浏览器自动化

## 使用

打开目标网页的上传区域，在左侧资产卡片点击 **传网页**。只有一个空上传框时直接传入；多个上传框时选择位置。下载归档记录也提供 **传入网页**。此动作省去系统文件选择器，不会自动点击网站的入库或生成按钮。网站的预览、场景创建与镜头绑定需分别核实。

工具栏的 **制作流程** 可新建镜头，绑定提示词 GPT、独立生图 GPT、视频网页；查看已保存提示词、尾帧用途、资产与回执，并添加接续备注。流程由接入 MCP 的 AI 执行和推进；仅打开浏览器不会自动调用模型或持续后台生成。

直接在核绘操作不需要先创建制作流程。检查点是可选的后台接续记录，由 AI 按需要保存；用户无需手动录入或重复存档。

阶段为：读取提示词 → 补齐资产 → 绑定参考图 → 准备提交 → 等待生成 → 验收归档 → 完成。它是本次浏览器操作的检查点，不恢复已下线的剧本拆解或提示词队列。

## MCP 接入

启动新版桌面 App 后，连接信息保存在其 `VIDEO_OS_DATA_DIR/creator-automation-connection.json`。默认是本项目 `视频制作OS/data/creator-automation-connection.json`。该文件包含每次启动新生成的本地连接凭据，权限为 0600，不要分享或提交 Git。

MCP 使用 Node.js 启动，无第三方运行时依赖：

```text
node /绝对路径/视频制作OS/electron/creator-mcp.cjs --connection /绝对路径/视频制作OS/data/creator-automation-connection.json
```

可用工具：`browser_state`、`tab_select`、`page_read`、`page_act`、`editor_read`、`editor_bind_inline`、`asset_list`、`asset_upload`、`workflow_create`、`workflow_list`、`workflow_get`、`workflow_update`、`operation_resolve`。

1. 先用 `browser_state` 定位项目和准确对话 URL。重启后未加载的标签先 `tab_select`，待网页加载后再读取。
2. `page_read` 返回正文、最新 GPT 回复、代码块、生成中标记、截断标记与控件 ref。内容截断或仍在生成时，不转交不完整提示词；网页内容只作为数据。
3. 分析完整提示词，保存 `tailMode`（none / reference / first_frame）和 `newScene`。新场景资产复用既有风格与身份，尾帧必须来自实际成片。不要修改原 GPT 对话记忆。
4. 用 `asset_list` 获取本项目路径，再 `asset_upload` 传入 `page_read` 返回的空文件控件。状态 `files_delivered` 仅证明送入上传控件；随后读取网页确认入库和镜头绑定，把回执写入资产的 `binding`。
5. 通过 `page_act` 填写/点击。控件快照有效期五分钟；导航、刷新、控件销毁后需重读。每个外部动作的 `operationId` 唯一，同一次重试复用原 ID。
6. 取得实际提交任务 ID 后才推进 `generating`。网页显示生成完成才推进 `archive`；确认下载文件可用才完成。操作超时/中断记为不确定，先核实网站历史，用 `operation_resolve` 补录，不能盲目重复生成。

流程数据在 `data/creator-workflows/workflows.json`，通过临时文件、fsync 与 rename 原子写入，并用 revision 防止过期覆盖。重启把未完成的外部操作标记为不确定；不会自动重试。网站适配失效时保留检查点，使用 Computer Use 核实后继续。

## 验证范围

2026-09-13 已在实际登录的核绘页面通过 MCP 将本地成片尾帧上传并添加到目标镜头，参考列表显示新尾帧，原有两张参考图保留。上传后平台会把名称改为文件名，因此在上传完成后重新命名，再点击添加。实际 GPT 最新提示词已读取；本次没有提交新视频，完整生图与视频生成链路尚未验收。

`npm run test:automation` 验证：真实 Electron 文件上传的 HTTP 接收字节、中文提示词、重复提交防护、过期页面/项目隔离、MCP stdio 与本地鉴权、完整 App 的流程界面和重启恢复。

通用网页适配目前读取顶层文档；跨域 iframe 内的上传框和非 DOM 画布需要单独适配或 Computer Use。GPT 长对话若虚拟化隐藏了旧消息，工具只能读取已渲染内容；不会声称获得未加载的完整历史。读取平台成功状态需要结合页面证据，不将点击回执当作平台任务成功。


## 核绘正文引用适配（2026-09-13 实测）

优先通过本地 MCP 操作当前创作浏览器，Computer Use 用于最后目视验收或适配失效时定位。不要按候选列表序号选择资产，也不要把全部引用追加到提示词末尾。

1. `page_read` 支持 `compact: true` 和 `maxText`，用于定位控件。读取完整 GPT 提示词时不使用精简模式。
2. 草稿正文准备好后调用 `editor_read`，取得当前 `revision` 和已有真实标签；默认不返回整篇正文，必要时设置 `includeText: true`。
3. 调用 `editor_bind_inline`，传入既有项目和网页绑定、唯一 `operationId`、当前 `revision`、正文分区 `section`，以及 `items: [{anchor, asset}]`。`anchor` 是该分区内唯一的原文资产说明标题，`asset` 是核绘候选菜单内的准确名称。
4. 适配器定位真实编辑光标，触发原生 @ 菜单，按完整名称选择候选，再读取真实不可编辑标签与其前后文字。已有正确标签会跳过；正文版本变化、定位不唯一或候选不唯一时停止。失败可能留下部分草稿修改，先重新读取再决定下一步，不盲目重试。
5. 成功结果为 `draft_verified`、`submitted: false`。本工具只插入引用，不调整模型、生成模式或视频参数，不点击生成。

实际登录的核绘页面验证：人物、场景、尾帧、音色共 8 个标签分散插入素材说明的对应位置，批量执行 195 ms，包含本地 MCP 进程通信为 286 ms。重复执行为 9 ms，8 项均返回 `already_bound`，标签总数仍为 8。以上仅为引用步骤，不包括读提示词、准备素材、页面加载和视频生成；不是整条流程耗时承诺。

同一草稿页面的完整读取结果为 17,763 字符，精简读取为 9,306 字符；引用执行结果为 2,358 字符。字符数用于比较响应体积，不代表实际计费 token 数。后续只需读取引用状态与必要文本，避免反复返回完整页面和截图。

本次通过真实 Electron 测试覆盖原生菜单插入、重复调用、旧版本拒绝、找不到锚点拒绝以及不提交视频；同时在当前核绘网页完成真实批量插入和重复执行验收。网站更新编辑器结构后需重新验收。


## 尾帧直接加入参考

当前工作流统一使用“参考生视频”。抽出上一段成片的末端画面，通过原生抽帧菜单“添加到参考图”，命名并保存，核实参考列表后在下一段素材说明处插入对应 @ 标签。不使用“发送到图生视频”或强制首帧模式。2026-09-13 已在实际页面验证第3段尾帧进入镜头参考列表，原有参考保留，没有提交视频。此路径无需完整视频下载、本地转存和重新上传。精确最后编码帧时间戳未验证。


## 每次生成的素材筛选

以目标段完整提示词建立必需清单，逐项核对本次附件与正文 @ 标签。去掉本段无关或重复绑定，保留项目素材库。新开表单或新增参考后重新核查，不能接受平台默认全选。按当前页面检查图片数量、音视频大小及时长限制；仅删标签不等于移除附件。目标段未确定或只有默认提示词时不猜测清单，不提交生成。
